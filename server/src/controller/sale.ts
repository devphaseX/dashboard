import { RequestHandler } from 'express';
import { OverAllStat } from '../model/OverAllStat';

type GetSalesRequestHandler = RequestHandler;

const getSales: GetSalesRequestHandler = async () => {
  try {
  } catch (e) {}
};

export { getSales };
